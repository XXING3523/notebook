**【二叉搜索树】**以序列顺序$A=\left \{a_1,a_2,\cdots,a_n  \right \}$（$a_i$为第$i$1个结点的权值）构建二叉搜索树，试给出时间复杂度为$O(n\log n)$的算法求解$a_i$对应结点父亲结点的权值$a_j$

试分析：对于待插入节点$a_i$,其父亲结点为$pre(a_i),next(a_i)$中的一个，若其中一个不存在，取另外一个，若均存在，取下标较小的

