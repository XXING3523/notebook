**【2025集英苑5月（网络联考）14】**已知$a>0$，若函数$\displaystyle f(x)=\frac{b}{e^x+a}$与$\displaystyle g(x)=\ln(\frac{b}{x}-a)$图像的对称中心重合，则$b$的最小值是？

**【基础】【函数基本性质】**若$\displaystyle f(x)=(x-\frac{a}{2}-1)\log_2(\frac{x-a}{b-x})$的图像存在对称轴，求$b$的值$\boxed{2}$

【2019高联A卷，6】
对任意闭区间 $I$，用 $M_I$ 表示函数 $y = \sin x$ 在 $I$ 上的最大值。若正数 $a$ 满足 $M_{[0,a]} = 2M_{[a,2a]}$，则 $a$ 的值为

【2023高联B卷，6】
将所有非完全平方的正奇数与所有正偶数的立方从小到大排成一列（前若干项依次为 $3,5,7,8,11,13,\cdots$），则该数列的第 $2023$ 项的值为

【2018江苏高考，14】
已知集合 $A = \{x|x = 2n - 1,n \in \mathbb{N}^*\}$，$B = \{x|x = 2^n,n \in \mathbb{N}^*\}$，将 $A \cup B$ 中所有元素从小到大依次排列构成一个数列 $\{a_n\}$。记 $S_n$ 为数列 $\{a_n\}$ 的前 $n$ 项和，则使得 $S_n > 12a_{n+1}$ 成立的 $n$ 的最小值为

【2015高联A卷，5】
在正方体中随机取 $3$ 条棱，它们两两异面的概率为

【2022全国甲，15】
从正方体的 $8$ 个顶点中任选 $4$ 个，则这 $4$ 个点在同一个平面的概率为

【2004湖南高考，文10理10】
从正方体的八个顶点中任取三个点为顶点作三角形，其中直角三角形的个数为
A. $56$
B. $52$
C. $48$
D. $40$

已知 $A_1,A_2,\cdots,A_n$ 是一个样本空间中的 $n$ 个事件。

(1) 证明Bonferroni不等式：
$$\displaystyle \mathbb{P}\left(\bigcup_{i=1}^n A_i\right) \ge \sum_{i=1}^n \mathbb{P}(A_i) - \sum_{1\le i<j\le n} \mathbb{P}(A_i \cap A_j).$$

(2) 设 $n \ge 2$，并且当 $i \neq j$ 时 $\mathbb{P}(A_i \cap A_j) \le \frac{1}{n}$。证明：存在与 $n$ 无关的常数 $c$，使得
$$\displaystyle \sum_{i=1}^n \mathbb{P}(A_i) \le c\sqrt{n}.$$

组合恒等式还可用于在概率统计中数学期望的估计.

已知一个口袋有 $m$ 个白球, $n$ 个黑球($m,n \in \mathbb{N}^*, n \ge 2$), 这些球除颜色外全部相同. 现将口袋中的球随机的逐个取出, 并放入如图所示的编号为 $1,2,3,\cdots,m+n$ 的抽屉内, 其中第 $k$ 次取球放入编号为 $k$ 的抽屉($k=1,2,3,\cdots,m+n$).

随机变量 $X$ 表示最后一个取出的黑球所在抽屉编号的倒数, $E(X)$ 是 $X$ 的数学期望. 接下来我们要估计 $E(X)$ 的值.

问题7: 写出 $X$ 的分布列与 $E(X)$ 的表达式 (用 $m,n$ 表示).

问题8: 通过 $\frac{k-1}{k} < 1$, 说明
$$\displaystyle E(X) < \frac{1}{\mathrm{C}_{m+n}^n} \sum_{k=n}^{m+n} \frac{(k-2)!}{(n-1)!(k-n)!}.$$

问题9: 仿照定理4的裂项相消过程, 利用定理3的恒等式对问题8的右边进行求和, 从而证明
$$\displaystyle E(X) < \frac{n}{(m+n)(n-1)}.$$



【2017江苏, 23】 已知一个口袋有 $m$ 个白球, $n$ 个黑球($m,n \in \mathbb{N}^*, n \ge 2$), 这些球除颜色外全部相同. 现将口袋中的球随机的逐个取出, 并放入如图所示的编号为 $1,2,3,\cdots,m+n$ 的抽屉内, 其中第 $k$ 次取球放入编号为 $k$ 的抽屉($k=1,2,3,\cdots,m+n$).

(1) 试求编号为2的抽屉内放的是黑球的概率 $p$;
(2) 随机变量 $X$ 表示最后一个取出的黑球所在抽屉编号的倒数, $E(X)$ 是 $X$ 的数学期望, 证明: $E(X) < \frac{n}{(m+n)(n-1)}.$

2. 集合之间的“差距”

我们为了衡量两个有限集合 $A,B$ 之间的“差距”，定义
$$\displaystyle d(A,B) = \mathrm{card}(A \cup B) - \mathrm{card}(A \cap B).$$

问题13: 设 $A = \{1,2,3,4,5\}$, $B = \{3,4,5,6\}$. 则 $d(A,B) = $__________.

问题14: 对任意有限集 $A,B$, “$A \neq B$” 是否为“$d(A,B) > 0$”的充分必要条件?

问题15: 对任意有限集 $A,B,C$, 是否有 $d(A,C) \le d(A,B) + d(B,C)$?

注: 形如 $d(A,C) \le d(A,B) + d(B,C)$ 的不等式叫三角不等式. 其直观理解是“三角形两边之和大于第三边”. 比如, 对于绝对值我们有类似的三角不等式 $|a-c| \le |a-b| + |b-c|$.

问题16: 仿照问题9, 用Venn图的方式给出 $\mathrm{card}(A \cup B \cup C)$, $\mathrm{card}(A)$, $\mathrm{card}(B)$, $\mathrm{card}(C)$, $\mathrm{card}(A \cap B)$, $\mathrm{card}(A \cap C)$, $\mathrm{card}(B \cap C)$, $\mathrm{card}(A \cap B \cap C)$ 满足的一个恒等式.

对于集合 $A = \{0,1,2,3,4,5,6\}$, $B = \{0,4,5,6,7,8\}$, $C = \{0,1,2,8,9\}$, 验证你写出的等式的正确性.

(*)式也可以推广到 $n$ 个集合的版本, 这个公式叫做容斥原理:
$$\displaystyle \mathrm{card}(S_1 \cup \cdots \cup S_n) = \mathrm{card}(S_1) + \cdots + \mathrm{card}(S_n) - \mathrm{card}(S_1 \cap S_2) - \cdots + \cdots + (-1)^{n-1}\mathrm{card}(S_1 \cap \cdots \cap S_n).$$

四、数论的筛法

在数论中, 容斥原理的思想贯穿在“筛法”中.

设 $x > 1$, 我们把位于闭区间 $[1,x]$ 的素数个数记为 $\pi(x)$. (这里 $\pi$ 不是圆周率)

问题17: 求 $\pi(7.5)$ 的值.

问题18: 设实数 $a,b$ 满足 $a < b$. 如何用函数 $\pi(x)$ 来表示位于区间 $(a,b]$ 的素数个数?

接下来解释埃拉托色尼(Eratosthenes)筛法的思想. 例如, 我们现在要用埃拉托色尼筛法寻找40以内的素数个数.

问题19: 写出40以内的素数. 一共有__________个素数、__________个合数. (注意1既不是素数也不是合数)

接下来请同学们按如下步骤进行操作: 首先把正整数1至40写在一个4行10列的方格上. 我们下面要把这个方格中的合数都划掉.

- 第一步, 划掉方格中除了2以外的2的倍数.
- 第二步, 划掉方格中除了3以外的3的倍数.
- 第三步, 划掉方格中除了5以外的5的倍数.

那么剩下的数中除了1以外都是40以内的素数.

记 $U = \{2,3,4,\cdots,40\}$ (我们不考虑1), $A_k$ 表示40以内能被 $k$ 整除且不为素数的正整数全体, $S_k = \mathrm{card}(A_k)$.

问题20: 求 $S_2,S_3,S_5,S_6,S_{10},S_{15},S_{30}$ 的值.

下面我们画出 $A_2,A_3,A_5$ 与 $U$ 构成的Venn图. 黄色表示 $A_2$, 红色表示 $A_3$, 蓝色表示 $A_5$.

问题21: $A_6$ 表示图中的哪个区域?

在第一步中, 我们划掉了集合 $A_2$ 中的元素, 即I+IV+V+VII.

在第二步中, 我们划掉了II+VI. 其中, IV+VII在第一步已经被划掉了, 这部分的正整数同时是2的倍数和3的倍数(即 $A_6$), 于是, 第二步结束后, 被划掉的数的个数是 $S_2 + S_3 - S_6$.

问题22: 在第三步中新划掉的是上面Venn图中的区域__________.

第三步结束后, 被划掉的数的个数用 $S_2,S_3,S_5,S_6,S_{10},S_{15},S_{30}$ 表示为________________________, 算出来的结果是__________.

利用问题16所得到的式子, 可知 $A_2 \cup A_3 \cup A_5$ 共有__________个元素, 这恰好是问题19中所求出来的__________的个数. 而剩下的未被划掉的数共有__________个, 恰好是问题19中所求出来的__________的个数.

注: 用埃拉托色尼筛法寻找不超过正整数 $N$ 的素数个数只需对不超过 $\sqrt{N}$ 的素数进行上述“划掉合数”步骤, 因此我们不需要再去判断集合 $A_7$ 的表现情况. 这在用计算机设计程序寻找小素数中非常常用, 可减少运行时间. 因此, 我们可以写出下面的等式:
$$\displaystyle \pi(N) = \mathrm{card}(U) - \mathrm{card}\left(\bigcup_{\text{素数}p \le \sqrt{N}} A_p\right).$$
而上式第二项可用容斥原理进行计算.

问题23: 为什么只需对不超过 $\sqrt{N}$ 的素数进行上述“划掉”步骤?



【2020新全国I, 5】 某中学的学生积极参加体育锻炼, 其中有96%的学生喜欢足球或游泳, 60%的学生喜欢足球, 82%的学生喜欢游泳, 则该中学既喜欢足球又喜欢游泳的学生数占该校学生总数的比例是

【2015浙江, 6】 设 $A,B$ 是有限集, 定义 $d(A,B) = \mathrm{card}(A \cup B) - \mathrm{card}(A \cap B)$, 其中 $\mathrm{card}(A)$ 表示有限集 $A$ 中的元素个数. 命题①: 对任意有限集 $A,B$, “$A \neq B$”是“$d(A,B) > 0$”的充分必要条件; 命题②: 对任意有限集 $A,B,C$, $d(A,C) \le d(A,B) + d(B,C)$. 下列判断正确的是

- A. 命题①和命题②都成立
- B. 命题①和命题②都不成立
- C. 命题①成立, 命题②不成立
- D. 命题①不成立, 命题②成立

【2006湖北, 8】(多选) 有限集合 $S$ 中元素的个数记作 $\mathrm{card}(S)$, 设 $A,B$ 都为有限集合, 给出下列命题, 其中真命题是

- A. $A \cap B = \varnothing$ 的充要条件是 $\mathrm{card}(A \cup B) = \mathrm{card}(A) + \mathrm{card}(B)$
- B. $A \subseteq B$ 的必要条件是 $\mathrm{card}(A) \le \mathrm{card}(B)$
- C. $A \subsetneq B$ 的充分条件是 $\mathrm{card}(A) < \mathrm{card}(B)$
- D. $A = B$ 的充要条件是 $\mathrm{card}(A) = \mathrm{card}(B)$

【2013重庆, 22】
对正整数 $n$, 记 $I_n = \{1,2,3,\cdots,n\}$,
$$\displaystyle P_n = \left\{\frac{m}{\sqrt{k}} \mid m \in I_n, k \in I_n\right\}.$$
(1) 求集合 $P_7$ 中元素的个数.
(2) 【选做】 若 $P_n$ 的子集 $A$ 中任意两个元素之和不是整数的平方, 则称 $A$ 为“稀疏集”. 求 $n$ 的最大值, 使 $P_n$ 能分成两个互不相交的稀疏集的并.
(提示: (2)问先证明 $n \ge 15$ 不成立, 再对 $n=14$ 的情形进行构造.)



已知集合 $\{a_1,a_2,\cdots,a_k\}(k \ge 2)$, 其中 $a_i \in \mathbb{Z}(i=1,2,\cdots,k)$.

由 $A$ 中的元素构成两个相应的集合:
$$\displaystyle S = \{(a,b) \mid a \in A, b \in A, a+b \in A\},$$
$$\displaystyle T = \{(a,b) \mid a \in A, b \in A, a-b \in A\}.$$
其中 $(a,b)$ 是有序数对, 集合 $S$ 和 $T$ 中的元素个数分别为 $m,n$. (有序数对可用平面直角坐标系上的点来类比)

若对于任意的 $a \in A$, 总有 $-a \notin A$, 则称集合 $A$ 具有性质 $P$.

问题1: 只有一个正整数构成的集合是否具有性质 $P$?

问题2: 说明集合 $\{-1,2,3\}$ 具有性质 $P$, 并写出相应的集合 $S$ 和 $T$.

问题3: 说明集合 $\{0,1,2,3\}$ 不具有性质 $P$.

相信同学们对这个问题的一些记号有了基本的了解. 我们下面探究性质 $P$ 的一些推论. 首先我们证明满足性质 $P$ 的集合必定有 $n \le \frac{k(k-1)}{2}$.

问题4: 由 $A$ 中元素构成的有序数对共有多少个?

问题5: 满足性质 $P$ 的集合中是否包含0?

问题6: 说明满足性质 $P$ 的集合 $A$ 对应的集合 $T$ 不包含 $(a_i,a_i)$.

问题7: 说明满足性质 $P$ 的集合 $A$ 对应的集合 $T$ 中, $(a_i,a_j)$ 与 $(a_j,a_i)$ 中只包含一个.

问题8: 对任何具有性质 $P$ 的集合, 证明:
$$\displaystyle n \le \frac{k(k-1)}{2}.$$
